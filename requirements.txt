# Python dependencies for OFFLINE CNN TRAINING (venv only).
# The Webots controller runs on system Python and only needs numpy + Pillow,
# which should be installed globally:  pip install numpy Pillow
#
# To set up the training venv:
#   python -m venv .venv
#   .\.venv\Scripts\activate
#   pip install -r requirements.txt
#
numpy>=1.24
torch>=2.0
torchvision>=0.15
Pillow>=10.0
